apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    sourceSets {
        main {
            java.srcDirs = ['src/main/java', 'src/main/viewgroupevent']
        }
    }
}

sourceCompatibility = "7"
targetCompatibility = "7"
